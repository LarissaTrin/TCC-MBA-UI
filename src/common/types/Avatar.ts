export interface GenericAvatarProps {
  /** The full name to be used for generating the avatar's initials and background color. */
  fullName: string;
  /** Optional. The size of the avatar in pixels (width and height).
   *
   * @default 40
   */
  size?: number;
}
