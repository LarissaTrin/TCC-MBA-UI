export enum GeneralColor {
  Primary = "primary",
  Secondary = "secondary",
  Success = "success",
  Error = "error",
  Info = "info",
  Warning = "warning",
}

export enum DefaultColor {
  Default = "default",
}

export enum InherittColor {
  Inherit = "inherit",
}
